# 
# Definition of all trackable objects
# Identifier corresponds to Trackable ID set in Tracking Tools
#
rigid_bodies:
    '1':
        pose: mavros/vision_pose/pose
        pose2d: vtol/ground_pose
        child_frame_id: fcu
        parent_frame_id: world
    '2':
        pose: polaris/pose
        pose2d: polaris/ground_pose
        frame_id: platform
        parent_frame_id: world
